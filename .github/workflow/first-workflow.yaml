name: First Workflow

on: [push]

jobs:
  rus-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: Multiline Command
        run: |
          echo "🐂"
          echo "🐇"
